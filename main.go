// hivemind — a general-purpose production multi-agent swarm for Claude Code.
// A single static binary: CLI + control plane + live TUI dashboard. Start with
// `hivemind setup`.
package main

import "hivemind/cmd"

func main() { cmd.Execute() }
